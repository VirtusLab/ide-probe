include required(classpath("ideprobe.conf"))

probe {
  workspace.path = "https://github.com/sbt/librarymanagement"
}

modules {
    verify = ["librarymanagement-core", "librarymanagement-ivy", "LM Root"]
    test = ["librarymanagement-core"]
}

runner = "scalatests"
