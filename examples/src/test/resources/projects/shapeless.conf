include required(classpath("ideprobe.conf"))

probe {
  workspace.path = "https://github.com/milessabin/shapeless"
}

modules {
    verify = ["core-sources", "coreJS", "scratch-sources", "scratchJS"]
    test = ["examples-sources"]
}