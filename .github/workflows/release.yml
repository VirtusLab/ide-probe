name: Release

on:
  push:
    branches:
      - master
    tags:
      - '**'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: release
        run: sbt ci-release
