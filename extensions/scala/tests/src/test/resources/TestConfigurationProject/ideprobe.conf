include required(classpath("scala.conf"))

probe {
    workspace.path = "classpath:/TestConfigurationProject"
}

test {
    module = "main"
    package = "com.example"
    class = "com.example.TestA"
    method = "should passA"
}
