probe {
  workspace.path = "classpath:/SbtProject"

  driver {
    headless = false
    launch.timeout = "600 seconds"
  }

  intellij {
    plugins = [
      // TODO: fix to work with: { id = "scala", repository.path = "https://github.com/JetBrains/intellij-scala" }
      { id = "org.intellij.scala", version = "2020.2.659", channel = "nightly" }
    ]
  }
}