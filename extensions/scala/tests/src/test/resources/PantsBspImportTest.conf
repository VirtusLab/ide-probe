probe {
  driver.headless = false
  intellij {
    plugins = [
      { id = "scala", repository.path = "https://github.com/JetBrains/intellij-scala" }
      // works with:
      // { id = "org.intellij.scala", version = "2020.2.659", channel = "nightly" }
    ]
  }

  workspace.path = "classpath:/PantsExample"
}
